 name: Setup and run
  run: |
    # Установка Яндекс.Музыка (если требуется)
    # Запуск с параметрами отладки
    Start-Process -FilePath "yandexmusic.exe" -ArgumentList "--remote-debugging-port=9222"
    Start-Sleep -Seconds 10
    node setup.js
  shell: pwsh
